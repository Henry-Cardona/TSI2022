
pipeline {
    agent any 
    stages {
        stage('test') { 
            steps {
                dir("TSI_WEB_AccessData"){
                    sh "mvn clean verify"
                } 
            }
        }
    }
}
