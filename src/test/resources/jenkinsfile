
import groovy.json.JsonSluperClassic

def jsonParse(def json){
    new groovy.json.JsonSluperClassic().parseText(json)
}


pipeline {
    agent { label 'master'}
    stages {
        stage('test') { 
            steps {
                dir ("src/test/resources/jenkinsfile") {
                    sh "mvn clean verify" 
                }
            }
        }
    }
}
